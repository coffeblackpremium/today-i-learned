# Array Manipulation with for

in javascript (and most other languages ​​as well) we can use to explore an array to get information

Example

```javascript
const myArray = [10, 15, 20, 30]

for(let i = 0; i < myArray.length; i++){
    console.log(myArray[i])
}
```